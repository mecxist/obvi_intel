# obvi_intel — obvious.md

## Repository

- **Repo:** mecxist/obvi_intel
- **Default branch:** main
- **Status:** Currently empty — the only tracked file is `README.md` (a bare title).

## What this file is

Top-level agent guidance for working in this repository. It is intentionally
minimal right now because there is no application code, no dependency manifest,
no Dockerfile/Compose/Makefile, and no documented stack to describe.

## Current state

- No dependency manifest (package.json / go.mod / Cargo.toml / pyproject.toml / Gemfile …)
- No Dockerfile, Compose file, or Makefile
- No source files, no tests, no CI configuration
- No `.gitignore`

## Not applicable yet

- Stack: none declared
- Commands: none — there is nothing to install, build, run, or test
- Ports: none
- Env vars: none

## Future runs

The next onboarding/setup run should replace this file with real content:
stack description, install/build/run commands, codebase map, and local
verification steps — once actual code and manifests exist in the repo.

## Snapshot

- No sandbox snapshot was taken for this run. The repo is empty; there was no
  dev stack to capture. `snapshotId: null`.
