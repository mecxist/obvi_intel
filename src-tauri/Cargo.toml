[package]
name = "obvious-intel-wrapper"
version = "0.1.0"
description = "Builder for an Intel-compatible desktop wrapper for Obvious on x86_64 Macs"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.6.1", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2.11.1", features = [] }

[target.'cfg(target_os = "macos")'.dependencies]
block2 = "0.6"
objc2 = "0.6"
objc2-foundation = { version = "0.3", features = ["NSString"] }
